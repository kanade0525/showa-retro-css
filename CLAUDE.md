# 昭和レトロ.css

昭和（おおむね1955〜1985）の実物を CSS だけで再現する部品集。
JavaScript も画像も使わない。意匠はすべてグラデーションと埋め込み SVG。

## 何より先に読むこと

**このプロジェクトの価値は「考証の誠実さ」にある。** README には
「実在しない意匠を混ぜていた。撤去した」「これは代用です」と自分の誤りが
書いてある。見た目が良いかより、**実物としてあり得るか**が優先される。

だから次の二つは別の話として扱う。

- 部品が正しく描けているか（腕金が金具に見えるか）
- **その置き方が実物にあるか**（袖看板を壁の正面に置くと宙に浮く）

後者を確認せずに「直った」と言わないこと。

## 作業の型

```sh
npm run build     # src/ 14ファイル → showa-retro.css + dist/
npm run check     # 下の4検査。CI でも走る
npm run audit     # 描画して実測。合成ボールドと端末依存フォントを見る
npm run shots     # 全18節を撮って docs/review/index.html に一覧化
npm run dev       # http://localhost:8765/ 手元で見る
```

**直す前に必ず `npm run shots` を回して自分で全部見る。** 利用者に不具合を
探させない。一件ずつ指摘させて一件ずつ直すのは、一度やって丸一日溶かした。

**公開して確かめない。** `npm run dev` で見る。GitHub Pages はビルド待ちと
キャッシュで一往復数分かかるうえ、push しても**自動でビルドが起動しない**。
必要なら `gh api -X POST repos/kanade0525/showa-retro-css/pages/builds`。

**まとめて直して一度に出す。** 一件ごとに tag / release / デプロイをすると
作業より公開の手間が大きくなる。

## 検査（`npm run check`）

| 検査 | 見ているもの |
| --- | --- |
| `build --check` | 生成物が `src/` と一致しているか |
| `contrast --check` | README のコントラスト表が実測値と合っているか |
| `check-docs` | ドキュメントのクラス名・変数名が実在するか |
| `check-glyphs` | 見本帳の地の文に晩秋レトロミンの収録外の字がないか |

**新しい種類の不具合を見つけたら、直すだけでなく検査を足す。**
同じものを二度探さないため。

## 書体

- `--sw-mincho` の先頭は**晩秋レトロミン**。地の文にも使う。このフレーム
  ワークの顔なので見出しに閉じ込めない
- 収録は**1732字**。範囲外は Zen Old Mincho に落ちて混植になる
- 見本帳の地の文では収録外の字を使わない。ただし**用語14字は例外**
  （色名 `墨` `橙` `朱` ／ クラス名 `枠` `袖` `傍` `蛍` `紋` `稿` `欄` `升`
  `縁` `袋` `掲`）。かな書きにすると混植より読みにくい
- **レトロミンは weight 400 しか無い。** 太字を要求すると合成ボールドになり、
  落ちた字は本物の太字で出るので太さがばらつく
- `--sw-mono` は Monaco 先頭。ただし桁揃えが要る `.sw-aozake` だけ
  M PLUS 1 Code を先に置く（Monaco の空白 0.6em と和文 1.0em で 2:1 が崩れる）

## 色

三層。**混ぜると夜間モードで壊れる。**

1. 紙と墨（`--sw-paper` `--sw-sumi`）… 昼夜で入れ替わる
2. 刷り色（`--sw-enji` ほか8色）… 昼夜で明度を調整
3. 物体色（`--sw-o-*`）… **昼夜で変えない。**実物の塗りだから

**地色をテーマ変数から取って文字色を物体色で固定すると、夜に潰れる。**
`:hover` で一度これをやった（全8色が AA 割れ、最悪 2.54）。押し色は
`--sw-o-*-oshi` に置いてある。

## バージョン

semver を守る。クラス・変数の削除、見た目の変更は破壊的変更。
出典は `package.json` 一つで、`index.html` の表記が食い違うとビルドが落ちる。

## やりがちな失敗

- **「直した」と言う前に確かめていない。** 一括置換が空振りしたのに削除したと
  報告した。computed style や描画を実際に読むこと
- **消して済ませる。** 判子は重複だったので削除が正解だったが、袖看板は
  再現の質の問題で、**消すのは答えではなかった**。区別すること
- **効いたかだけ見て、結果が正しいか見ない**
