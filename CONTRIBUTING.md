# 直すときの手順

意匠の不具合を一つずつ指摘して、一つずつ直して、一つずつ公開する——
という進め方を一度やって、丸一日かかりました。その反省から決めた手順です。

## 1. まず全部撮る

```sh
npm run shots            # 昼夜ぶん
npm run shots -- --light # 昼だけ（速い）
npm run shots -- --narrow # 狭い画面も
open docs/review/index.html
```

見本帳の全節を撮って、一覧ページにまとめます。**画面を上から順に見て、
気になった所を「節番号 + 何が変か」で書き出してください。** 一件ずつ
渡すより、まとめて渡す方が何倍も速く終わります。

`docs/review/` は作り直せるので commit しません。

## 2. 機械で分かるものは機械に出させる

```sh
npm run check
```

四つ走ります。**人が目で探す必要のないもの**は、ここで落ちます。

| 検査 | 見ているもの |
| --- | --- |
| `build --check` | `showa-retro.css` と `dist/` が `src/` と一致しているか |
| `contrast --check` | README のコントラスト表が実測値と合っているか |
| `check-docs` | ドキュメントに書いたクラス名・変数名が実在するか |
| `check-glyphs` | 見本帳の地の文と CSS の `content:` に収録外の字がないか |

`npm run check` は静的解析なので CI で回せますが、**描画しないと分からないこと**が
あります。公開前に `npm run audit` を実行してください（Chrome が要ります）。

| 検査 | 見ているもの |
| --- | --- |
| 合成ボールド | レトロミンは400のみ。CSS から探すと継承する要素を取りこぼす |
| 端末依存フォント | 撮影した絵を公開時の見え方と取り違えないため |

過去に見つかった不具合は、だいたいこの四つのどれかで検出できます。
新しい種類の不具合を見つけたら、**直すだけでなく検査を足してください。**
同じものを二度探さないためです。

## 3. 変わった所だけを見る

```sh
npm run shots -- --all   # 昼夜・狭い画面・強制配色を全部撮る
npm run visual           # 基準と食い違う節を挙げる
npm run baseline         # 意図した変更なら基準を取り直す
```

意匠を一つ直すと意図しない所まで動きます（合成ボールドを止めたら見出しが
全部細くなり、腕金を作り直したら看板の面に腕が乗りました）。全部を毎回
見直すのは現実的でないので、**承認した描画をハッシュで基準として持ち、
次からは差分だけ見ます。** 画像そのものは置きません（90枚で20MB超）。

Chrome の版が上がると全部差分として出ます。そのときは撮り直して
`npm run baseline` で取り直してください。

## 4. 手元で見る。公開して確かめない

```sh
npm run dev   # http://localhost:8765/
```

GitHub Pages に上げてから確かめると、ビルド待ちとキャッシュで
一往復あたり数分かかります。しかも Pages のビルドは push しても
自動で起動しないことがあります。**手元で見てください。**

## 5. まとめて直して、一度に公開する

一件ごとに commit / tag / release / デプロイをすると、作業より
公開の手間の方が大きくなります。枝を切って直しを溜め、通してから
一度に出してください。

```sh
git switch -c fix/ishou
# …直す…
npm run build && npm run check
npm run shots -- --light   # 直った所と、壊していない所を目で確認
git switch main && git merge fix/ishou
```

## 見落としやすい所

一日で実際に見つかったものです。同じ種類はまた出ます。

- **指定が効いていない。** `code` `pre` `input` などは要素側が既定値を
  持っていて、親からの継承より強い。詳細度で負けていることもある
  （`.d-sode` の `position` が `.sw-sodekanban.is-udegane` に負けていた）。
  **computed style を実際に読んで確かめること。**
- **合成ボールド。** 晩秋レトロミンは weight 400 しか持たない。
  太字を要求すると偽の太字になり、収録外で落ちた字は本物の太字で出る。
- **夜間モードのコントラスト。** 地色をテーマ変数から取って文字色を
  物体色で固定すると、夜に近づいて潰れる。`:hover` は特に見落としやすい。
- **実物として成立しない配置。** 袖看板を壁の正面に置くと宙に浮く。
  「部品が正しく描けているか」と「その置き方が実物にあるか」は別の話。
- **意味のない飾り。** 部品を意味なく置いていないか。判子は承認の印で、
  自分の店先に押すものではない。

## Claude Code の設定

このリポジトリには次が入っています。会話をまたいでも同じ手順で動くようにする
ためのものです。

| ファイル | 役割 |
| --- | --- |
| `CLAUDE.md` | 毎回読まれる。考証の方針・書体・色・失敗の型 |
| `.claude/skills/design-review/` | `/design-review` で意匠を一括検分する手順 |
| `.claude/agents/ishou-reviewer.md` | 一節ぶんを検分する専用の担当。並列に走る |
| `.claude/settings.json` | `src/` などを触ったら `npm run build && npm run check` を自動で回す |

**新しい考証（実物の要件）が分かったら `SKILL.md` の表に追記してください。**
そこに書いていないことは、次の会話では失われます。

## バージョンの付け方

[セマンティック バージョニング](https://semver.org/lang/ja/)に従います。
クラスや変数を消す、見た目が変わる——は破壊的変更です。番号を上げてください。
出典は `package.json` の一つだけで、`index.html` の表記が食い違うと
ビルドが落ちます。
